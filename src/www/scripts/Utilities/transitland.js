/**
 * This script file SHOULD NOT BE directly imported from the HTML File,
 *   Refer to 'api.js' for inclusion logic
 */

 
function getRouteStop(){

}

function getData(ver){
    var endpoint = 'https://transit.land/api/v' + ver;

    $.ajax(

    );
}